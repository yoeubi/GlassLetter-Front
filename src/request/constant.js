const ENDPOINT = "http://localhost:5000"
const routes = {
  "send_message": ENDPOINT+"/sendmessage",
  "view_message": ENDPOINT+"/viewmessage",
  "register" : ENDPOINT+"/register",
}


export default routes;
